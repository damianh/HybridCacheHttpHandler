name: signatures/release

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Version number (x.y.z or x.y.z-prerelease.1)'
        required: true
        type: string

jobs:
  release:
    uses: ./.github/workflows/release-template.yml
    with:
      product: signatures
      package: DamianH.Http.HttpSignatures
      tag-prefix: sig-v
      version: ${{ inputs.version }}
    secrets:
      NUGET_ORG_APIKEY: ${{ secrets.NUGET_ORG_APIKEY }}
